# src/

Contains the `synthobs` Python package — the tested, stdlib-only engine that
is the verified source of truth for SynthOBS/FractiSynth. Module map and
invariants: `synthobs/README.md`; coverage gate is 90% (see `../pyproject.toml`).

Part of the DataTools lane (local-only, never committed). Parent: `../README.md`.

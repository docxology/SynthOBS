# locale/

OBS localization strings for the FractiSynth plugin: `en-US.ini` provides the
filter/source/property display names shown in the OBS UI (loaded via
`obs_module_get_string` conventions).

Part of the DataTools lane (local-only, never committed). Parent: `../README.md`.

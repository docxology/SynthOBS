# AGENTS.md — `SynthOBS/manuscript/assets`

> Local-only path under `projects/ongoing/DataTools/` — matched by the
> root `.gitignore` rule `projects/*`; never commit. Repo-wide policy:
> see `/Volumes/external_drive/Git/template/projects/ongoing/AGENTS.md`.

## What this is

Versioned manuscript asset bundles. The only child, `obs/`, is the promoted
live-OBS evidence bundle from acceptance run `20260717T153649Z` (six gates
passing against OBS 32.1.2; full record in `obs/obs_manifest.json`,
human summary in `obs/README.md`). Do not hand-edit the capture files or
manifest: they are byte-frozen evidence, and `scripts/promote_obs_evidence.py`
recomputes hashes/dimensions/WAV metadata on every promotion. Analytical
figures are generated artifacts under `../../output/figures/`, not assets.

## Layout (verified by direct listing, 2026-08-29)

```
obs/
```

## Gotchas

- Contents-only docs: this file was generated by the fleet pass; if the
  tree changed since, re-verify against disk.
- Never `git add` anything under `projects/ongoing/`.
